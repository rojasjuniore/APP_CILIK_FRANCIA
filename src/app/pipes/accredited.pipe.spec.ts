import { AccreditedPipe } from './accredited.pipe';

describe('AccreditedPipe', () => {
  it('create an instance', () => {
    const pipe = new AccreditedPipe();
    expect(pipe).toBeTruthy();
  });
});
