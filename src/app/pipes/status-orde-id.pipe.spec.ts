import { StatusOrdeIDPipe } from './status-orde-id.pipe';

describe('StatusOrdeIDPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusOrdeIDPipe();
    expect(pipe).toBeTruthy();
  });
});
